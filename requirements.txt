# Core deep-learning stack
torch>=2.1
torchaudio>=2.1
timm>=0.9.16

# Audio I/O and numerics
soundfile>=0.12
librosa>=0.10
joblib>=1.3
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
tqdm>=4.65

# Knowledge distillation teacher (Perch v2 is a TF SavedModel)
# Install the GPU build matching your CUDA version; CPU works but is slow.
tensorflow>=2.15
